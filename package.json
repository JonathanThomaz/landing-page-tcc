{
  "name": "tcc-lading-page",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server public",
    "build": "tsc --outDir public/js",
    "watch": "tsc --watch --outDir public/js",
    "dev": "npm run build && concurrently \"npm run watch\" \"npm run start\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  }
}
